/**
 * ES: Posición del cubo interactivo en `FrameworkDemoGround` — el servidor usa la misma para validar distancia.
 * EN: Demo cube position; server uses the same coords for distance checks.
 */
export const FRAMEWORK_DEMO_CUBE_CENTER = { x: 3, y: 0.5, z: 0 } as const;
