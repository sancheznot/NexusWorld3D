#!/usr/bin/env node
/**
 * ES: Placeholder Fase 2 — aquí irá validación del manifest de contenido (IDs, refs).
 * EN: Phase 2 placeholder — future content manifest validation (IDs, refs).
 */
console.log("[validate-content] OK (no manifest schema wired yet)");
process.exit(0);
