/** ES: Subconjunto de jugador para HousingEvents (sin importar NexusWorldRoom). */
export type PlayerData = {
  username: string;
  mapId: string;
  position: { x: number; y: number; z: number };
};
